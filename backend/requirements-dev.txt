# Test-only dependencies for the backend suite.
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
httpx>=0.27          # used by Starlette's TestClient
websockets>=12.0     # used by TestClient.websocket_connect
